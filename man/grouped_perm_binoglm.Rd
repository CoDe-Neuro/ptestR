% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped_perm_binoglm.R
\name{grouped_perm_binoglm}
\alias{grouped_perm_binoglm}
\title{Permutation test for binomial logistic regression}
\usage{
grouped_perm_binoglm(tbl, formla, var_to_perm, permNum = 1000, seed = 42)
}
\arguments{
\item{tbl}{A data frame or tibble containing all model variables.}

\item{formla}{A \link{formula} describing the logistic regression model;
passed to \code{\link[stats:glm]{stats::glm()}} with \code{family = binomial}.}

\item{var_to_perm}{Character. Name of the binary outcome column to permute.}

\item{permNum}{Integer. Number of permutations to generate. Default \code{1000}.}

\item{seed}{Integer. Random seed for reproducibility; passed to
\code{\link[base:Random]{base::set.seed()}}. Default \code{42}.}
}
\value{
A tibble with one row per model term and columns:
\describe{
\item{\code{term}}{Name of the regression term.}
\item{\code{estimate}}{Estimated log-odds coefficient.}
\item{\code{statistic}}{Observed z-statistic.}
\item{\code{p.value}}{Asymptotic two-sided p-value from the fitted model.}
\item{\code{p.perm}}{Permutation p-value: proportion of permuted |statistics|
>= |observed statistic|. A value of \code{0} means no permuted statistic
was as extreme; report as \code{p < 1/permNum}.}
}
}
\description{
\code{grouped_perm_binoglm} is a nonparametric test for binomial
logistic regression. It assesses the significance of coefficients by
permutation, computing the null distribution of the z-statistic by
randomly rearranging the binary outcome variable.
}
\examples{
counts <- c(10, 11, 8, 9, 6, 3, 5, 1)
gender <- c(0, 0, 0, 0, 0, 1, 1, 1)
TBL    <- data.frame(counts, gender)
grouped_perm_binoglm(TBL, gender ~ counts, "gender", permNum = 500, seed = 1)
}
